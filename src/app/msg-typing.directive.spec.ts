import { MsgTypingDirective } from './msg-typing.directive';

describe('MsgTypingDirective', () => {
  it('should create an instance', () => {
    const directive = new MsgTypingDirective();
    expect(directive).toBeTruthy();
  });
});
