import { MsgWrapperDirective } from './msg-wrapper.directive';

describe('MsgWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new MsgWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
