import { FileSelectionDirective } from './file-selection.directive';

describe('FileSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new FileSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
